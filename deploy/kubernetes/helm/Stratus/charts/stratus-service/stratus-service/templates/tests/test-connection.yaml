apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stratus-service.fullname" . }}-test-connection"
  labels:
{{ include "stratus-service.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "stratus-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
